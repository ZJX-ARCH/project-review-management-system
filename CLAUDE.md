# Claude Code 项目开发规范

> ContiNew Admin - 项目评审管理系统 | V4.0 协作工作流

---

## 🚨 强制公约（必须遵守）

### 输出规范
1. **必须使用中文输出**：所有回复、注释、文档必须使用中文
2. **禁止使用颜文字**：代码和文档中严禁使用任何 emoji 或颜文字
3. **代码注释规范**：使用中文注释，公共方法必须有 JSDoc/Javadoc

### 任务交接规范
4. **不能直接写代码**：给 Gemini/Codex 的任务书中只能写需求说明和思路，不能写具体实现代码
   - ✅ 可以：提供实体类结构、接口定义、业务逻辑描述
   - ❌ 禁止：写完整的 Service/Controller 实现代码
5. **每次完成任务后必须重新阅读工作流**：确认下一步该做什么，不能自己瞎写

### Git 规范
6. **Git 提交信息必须使用中文**
7. **及时提交临时文件**：`docs/dev/` 下的任务文件也要提交到 Git
8. **清理杂志文件**：发现 `.patch`、临时文件等要及时删除
9. **配置 .gitignore 和 .gitattributes**：防止错误提交
10. **仅项目级提交**：根目录不需要提交 Git，只在各子项目（continew-admin、continew-admin-ui、continew-starter）中提交

### 代码规范
11. **Service 层规范**：Service 必须是接口，ServiceImpl 才是实现类
12. **工具方法独立**：Controller 中不能堆砌工具方法，要独立成 `XxxUtils` 工具类
13. **多使用 Lombok**：减少样板代码
14. **代码整洁**：保持代码结构清晰，不能混乱

---

## 🔄 V4.0 协作工作流

### 核心原则

1. **P0 用户评审**：架构设计必须经过用户确认
2. **迭代式三方对抗**：Stage 1 使用 Designer → Critic 循环对抗，直到收敛后由 Architect 终审
   - **Designer (Gemini)**：创建/改进设计
   - **Critic (Codex)**：审查并挑战设计
   - **持续迭代**：重复对抗直到没有重大问题（P0/P1）
   - **Architect (Opus)**：最终审查和确认
3. **渐进式休眠**：Claude Code 主控制器在等待后台任务时使用渐进式休眠策略
4. **串行执行**：一次只执行一个任务
5. **立即提交**：测试通过后立即 Git 提交
6. **四层记忆更新**：实时更新 QUICK_RESUME.md 等
7. **交接文档**：每次调用 Agent 前准备交接文档
8. **不中断执行**：持续工作直到任务完成，不要中途停下来要求批准
9. **每个模块后进行三方对抗测试**

### 渐进式休眠策略（重要）

**关键规则**：监督者休眠，工作者不休眠
- **Claude Code（监督者）**：在等待 Gemini/Codex 完成任务时使用休眠
- **Gemini/Codex（工作者）**：执行任务时不能休眠，必须持续工作

**休眠流程**：
```bash
# Claude Code 启动后台任务
cat "D:\project-review-management-system\docs\dev\TASK.txt" | gemini --yolo &

# Claude Code 休眠等待（渐进式增加）
sleep 5
# 检查任务状态，如未完成继续休眠
sleep 10
# 再次检查...
sleep 30
# 持续检查直到任务完成
```

### 模型分工

- **Opus 4.5**：P0 架构设计、Stage 1 Architect 终审
- **Gemini 3.0 Pro**：Stage 1 Designer、前端实现
- **Codex 5.1**：Stage 1 Critic、后端实现
- **Sonnet 4.5**：主控制、常规任务、代码审查
- **Haiku 4.5**：Stage 8 Git 提交

### 模型调用方法（使用全路径）

**Gemini 调用**：
```bash
cat "D:\project-review-management-system\docs\dev\DESIGNER_TASK.txt" | gemini --yolo
```
- `--yolo` 参数：自动批准所有工具调用（包括文件写入）

**Codex 调用**：
```bash
codex exec --dangerously-bypass-approvals-and-sandbox "$(cat 'D:\project-review-management-system\docs\dev\CRITIC_TASK.txt')"
```
- `exec` 子命令：非交互式执行
- `--dangerously-bypass-approvals-and-sandbox`：跳过所有确认并无沙箱执行

**Opus 调用**：
```typescript
// 使用 Claude Code Task tool
Task({
  model: "opus",
  prompt: "任务描述"
})
```

**Haiku 调用**：
```typescript
// 使用 Claude Code Task tool
Task({
  model: "haiku",
  prompt: "任务描述"
})
```

---

## 📋 项目概述

### 项目简介

**ContiNew Admin（Continue New Admin）** - 项目评审管理系统

一个前后端分离的中后台管理系统框架，基于 Spring Boot 3.3.12 + Vue 3.5.4 + Arco Design 2.57.0 构建。

### 用户角色

1. **申请人**：提交项目申请
2. **审核员**：审核验证申请
3. **专家**：评估打分项目
4. **管理员**：管理系统运营
5. **系统管理员**：系统配置和维护

### 仓库结构

```
project-review-management-system/
├── continew-starter/        # 基础框架（Spring Boot Starter 集合）
├── continew-admin/          # 后端项目（Spring Boot 应用）
└── continew-admin-ui/       # 前端项目（Vue 3 应用）
```

### 项目依赖关系

```
continew-starter（基础框架）
    ↓ 依赖
continew-admin（业务应用后端）
    ↓ API
continew-admin-ui（业务应用前端）
```

---

## 🛠 技术栈

### 后端技术栈

- **框架**：Spring Boot 3.3.12 + ContiNew Starter 2.14.0
- **Web 服务器**：Undertow 2.3.18
- **认证授权**：Sa-Token 1.44.0 + JWT
- **ORM**：MyBatis Plus 3.5.12
- **数据库**：MySQL 8.0.42
- **缓存**：JetCache 2.7.8 + Redis 7.2.8
- **任务调度**：Snail Job 1.5.0
- **文件存储**：X File Storage 2.2.1
- **接口文档**：Knife4j 4.5.0
- **工具类**：Hutool 5.8.38 + Lombok 1.18.36

### 前端技术栈

- **框架**：Vue 3.5.4
- **UI 组件库**：Arco Design 2.57.0
- **语言**：TypeScript 5.0.4
- **构建工具**：Vite 5.1.5
- **状态管理**：Pinia 2.0.16
- **路由**：Vue Router 4.3.3
- **HTTP 客户端**：Axios 0.27.2
- **图表**：ECharts 5.4.2

---

## 📁 项目架构

### ContiNew Starter 架构（基础框架层）

```
continew-starter/
├── continew-starter-core            # 核心模块
├── continew-starter-web             # Web 开发模块
├── continew-starter-api-doc         # 接口文档模块
├── continew-starter-validation      # 校验模块
├── continew-starter-cache/          # 缓存模块
├── continew-starter-auth/           # 认证模块
├── continew-starter-data/           # 数据访问模块
├── continew-starter-security/       # 安全模块
├── continew-starter-log/            # 日志模块
├── continew-starter-storage/        # 存储模块
└── continew-starter-extension/      # 扩展模块
```

### ContiNew Admin 架构（业务应用层）

```
continew-admin/
├── continew-server         # 打包部署模块（主应用入口）
│   ├── config/            # 配置类
│   ├── controller/        # 通用 API
│   ├── job/              # 定时任务
│   └── resources/
│       ├── config/       # 配置文件（application.yml）
│       └── db/changelog/ # Liquibase 数据库脚本
│
├── continew-system         # 系统管理模块
│   ├── auth/             # 认证相关业务
│   └── system/           # 系统管理功能
│
├── continew-plugin/        # 插件模块
│   ├── continew-plugin-tenant/     # 租户插件
│   ├── continew-plugin-schedule/   # 任务调度插件
│   └── continew-plugin-generator/  # 代码生成器插件
│
└── continew-common         # 公共模块
    ├── base/             # 基类（Controller、Service、Entity）
    ├── model/            # 公共模型
    ├── util/             # 公共工具类
    └── config/           # 公共配置
```

**业务模块内部结构**：
```
module/
├── controller/      # API 控制器
├── service/         # 业务逻辑接口
│   └── impl/       # 业务逻辑实现（重要：Service 是接口，Impl 是实现）
├── mapper/          # MyBatis Mapper 接口
└── model/
    ├── entity/     # 数据库实体
    ├── req/        # 请求参数
    └── resp/       # 响应参数
```

### 前端架构

```
continew-admin-ui/
├── src/
│   ├── apis/              # 请求接口（按模块划分）
│   ├── components/        # 通用业务组件
│   ├── router/            # 路由配置
│   ├── stores/            # Pinia 状态管理
│   ├── utils/             # 工具库
│   └── views/             # 页面视图
│       ├── dashboard/    # 仪表盘
│       ├── system/       # 系统管理
│       └── monitor/      # 系统监控
│
├── .env.development       # 开发环境变量
├── .env.production        # 生产环境变量
└── vite.config.ts         # Vite 配置
```

---

## 💻 常用开发命令

### ContiNew Starter 命令（如需修改框架）

```bash
# 进入 starter 目录
cd continew-starter

# 编译并发布到本地仓库
mvn clean install

# 代码格式化
mvn spotless:apply
```

### 后端命令

```bash
# 进入后端目录
cd continew-admin

# 编译项目
mvn clean compile -DskipTests

# 打包项目
mvn clean package -DskipTests

# 运行主应用
mvn spring-boot:run -pl continew-server
```

### 前端命令

```bash
# 进入前端目录
cd continew-admin-ui

# 安装依赖
pnpm bootstrap

# 启动开发服务器
pnpm dev

# 构建生产环境
pnpm build

# ESLint 自动修复
pnpm lint:fix
```

---

## 📝 开发规范

### 编码标准

- **编码格式**：UTF-8 without BOM
- **缩进**：前端 2 空格，后端 4 空格
- **命名规范**：camelCase
- **注释**：公共方法必须有 JSDoc/Javadoc（中文）
- **Lombok**：多使用 Lombok 注解减少样板代码

### 后端开发规范

1. **分层架构**：严格遵守 Controller → Service → Mapper 三层架构
2. **Service 层规范**：
   - Service 必须是**接口**
   - ServiceImpl 才是**实现类**
3. **工具方法独立**：Controller 中不能堆砌工具方法，要独立成 `XxxUtils` 工具类
4. **DTO 命名规范**：
   - 查询条件：`XxxQuery`
   - 请求参数：`XxxReq`
   - 响应参数：`XxxResp`
   - 数据库实体：`Xxx`（继承 `BaseEntity`）
5. **统一响应**：API 返回使用 `R<T>` 包装
6. **异常处理**：业务异常抛出 `BusinessException`
7. **权限注解**：使用 `@SaCheckPermission` 进行权限校验
8. **参数校验**：使用 Jakarta Validation 注解

### 前端开发规范

1. **目录结构**：页面放在 `views/` 对应模块下，API 接口放在 `apis/` 对应模块下
2. **组件命名**：使用 PascalCase
3. **API 调用**：统一使用 `apis/` 下定义的接口函数
4. **状态管理**：使用 Pinia
5. **类型定义**：TypeScript 类型定义放在 `types/` 目录
6. **样式规范**：组件样式使用 scoped

### 测试要求

- 单元测试覆盖率 ≥ 80%
- 核心业务必须有集成测试
- 使用 TDD 方法

---

## 🔑 关键开发要点

### 后端关键实现

1. **CRUD 基类继承**：
   - Controller 继承 `BaseController`
   - Service 继承 `BaseServiceImpl`
   - Mapper 继承 `DataPermissionMapper`

2. **数据库版本管理（Liquibase）**：
   - 所有数据库变更通过 Liquibase 管理
   - 变更脚本位于 `continew-server/src/main/resources/db/changelog/`
   - **重要**：不要修改已执行的 changeset，只能追加新的

3. **认证与鉴权**：
   - 使用 `@SaIgnore` 忽略认证
   - 使用 `@SaCheckPermission` 进行权限校验

### 前端关键实现

1. **路由管理**：
   - 静态路由在 `src/router/route.ts` 中定义
   - 动态路由通过后端接口 `/auth/user/route` 获取

2. **权限控制**：
   - 使用 `v-permission` 指令控制按钮显示
   - 使用 `hasPerm()` 函数进行权限判断

3. **数据字典**：
   - 使用 `useDict()` Hook 获取字典数据

---

## ⚠️ 注意事项

### 环境要求

- **Java 版本**：Java 17+
- **Maven 版本**：Maven 3.6+
- **Node 版本**：Node.js 18+
- **包管理器**：pnpm

### 运行配置

- **后端默认端口**：8000
- **前端开发端口**：5173
- **Redis 依赖**：启动前确保 Redis 服务已启动（默认 localhost:6379）
- **MySQL 依赖**：启动前确保 MySQL 8.0+ 服务已启动

### 本地开发工作流

1. **只开发业务功能**（不改 Starter）：
   - 启动 continew-admin 和 continew-admin-ui

2. **需要修改 Starter 能力**：
   ```bash
   # 1. 修改 continew-starter 代码
   cd continew-starter
   mvn clean install

   # 2. 重新编译 continew-admin
   cd ../continew-admin
   mvn clean compile

   # 3. 重启 continew-admin 应用
   ```

3. **接口文档访问**：http://localhost:8000/doc.html

---

## 📚 功能手册

详细功能使用手册位于 `continew-admin/.style/功能手册/` 目录，涵盖：
- 登录（账号、手机号、邮箱、第三方登录）
- 仪表盘（工作台、分析页）
- 系统管理（用户、角色、菜单、部门、字典、文件、通知）
- 系统配置（网站、安全、登录、邮件、短信、存储配置）
- 系统监控（在线用户、登录日志、操作日志）
- 租户管理、任务调度、代码生成
- 个人中心、消息中心

---

## 🚫 禁止操作

1. ❌ 不要跳过 P0 用户评审
2. ❌ 不要跳过三方对抗设计
3. ❌ 不要并行执行任务
4. ❌ 不要批量 Git 提交
5. ❌ 不要省略渐进式休眠指令
6. ❌ 不要在任务书中直接写实现代码
7. ❌ 不要使用颜文字
8. ❌ 不要用英文输出

---

*最后更新：2025-12-18*
